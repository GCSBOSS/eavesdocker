{
  "name": "eavesdocker",
  "version": "0.0.0",
  "main": "lib/main.js",
  "scripts": {
    "test": "mocha -b --no-diff test/*.js",
    "coverage": "nyc -r html -r text-summary mocha -b --no-diff --no-warnings test/*.js"
  },
  "dependencies": {
    "dockerode": "^3.2.1",
    "mongodb": "^3.6.5",
    "nodecaf": "^0.11.5",
    "nodecaf-redis": "0.0.1",
    "toml": "^3.0.0"
  },
  "devDependencies": {
    "muhb": "^3.0.4"
  }
}
